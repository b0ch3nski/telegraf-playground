# Resolve hostname and MAC address from DNSMasq DHCP leases
[[processors.dhcp]]
  # leases_file_path = "/tmp/dnsmasq/dhcp.leases"

  ## List of fields holding IP address to resolve
  # ip_fields = ["src", "dst", "made_by"]

  ## List of tags holding IP address to resolve
  # ip_tags = ["address"]
